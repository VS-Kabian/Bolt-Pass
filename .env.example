# .env.example - copy to .env and update values
DB_HOST=localhost
DB_USER=root
DB_PASS=
# Use the exact database name created by db-init.sql. The default here is `bolt-coin` (lowercase, hyphen).
DB_NAME=bolt-coin
PORT=3000
JWT_SECRET=change_this
# ENC_KEY should be a base64-encoded 32-byte key used to encrypt/decrypt stored passwords.
# Generate with Node: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENC_KEY=
